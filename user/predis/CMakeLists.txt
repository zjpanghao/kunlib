set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${KUN}libs)
add_definitions(-std=c++11)
include_directories(${KUN}/include ${KUN}/include/hiredis ${KUN}/user)
link_directories(${KUN}/libs)
add_library(predis SHARED redis_pool.cpp)
target_link_libraries(predis kunconfig glog)

set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${KUN}/libs)
add_definitions(-std=c++11)
include_directories(${KUN}/include)
link_directories(${KUN}/libs)
add_library(kunconfig SHARED config.cpp)
target_link_libraries(kunconfig glog)
